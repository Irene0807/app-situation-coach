import 'package:flutter/material.dart';

class PagePreTest extends StatelessWidget {
  const PagePreTest({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
      body: Center(child: Text('PagePreTest')),
    );
  }
}
