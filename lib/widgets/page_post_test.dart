import 'package:flutter/material.dart';

class PagePostTest extends StatelessWidget {
  const PagePostTest({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.transparent,
      body: Center(child: Text('PagePostTest')),
    );
  }
}
